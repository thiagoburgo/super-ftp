# Configuração do npm para garantir uso do registry público
# Este arquivo garante que o projeto sempre use o registry público do npm
# mesmo quando o desenvolvedor tenha configurado outro registry globalmente

# Registry público para dependências
registry=https://registry.npmjs.org/

# Scoped registry para pacotes @clin - CodeArtifact
@clin:registry=https://clin-180294188860.d.codeartifact.us-east-2.amazonaws.com/npm/clin/
